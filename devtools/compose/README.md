This directory contains Dockerfiles and needed config files for the following services:

- Elasticsearch
- PostgreSQL
- Redis
- PGAdmin (database web management interface)

All these files are referenced from docker-compose.yml (see main README.md file)